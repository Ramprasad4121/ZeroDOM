# ZeroDOM Build Specification Pointer

The canonical build specification lives at:

`docs/ZeroDOM — Build Specification`

This root-level pointer exists because `docs/AGENTS.md` refers to
`zerodom-build-spec.md` as the source-of-truth handoff file.


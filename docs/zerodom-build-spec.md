# ZeroDOM Build Specification Pointer

The canonical build specification lives in this same directory at:

`ZeroDOM — Build Specification`

This lowercase pointer exists for tools that resolve the `docs/AGENTS.md`
handoff reference relative to the `docs/` directory.
